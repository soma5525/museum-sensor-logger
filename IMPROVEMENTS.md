# 今後の改善案

このファイルに、博物館センサ記録アプリの改善案と検討状況を追記する。
「候補」は未実装・未承認の案であり、実装する範囲はその都度確認する。

## 2026-09-11 時点の状況

- 施設別のセンサ数は設定駆動。国立科学博物館20地点、神奈川県立生命の星・地球博物館10地点、東京国立博物館10地点。
- 記録設定の「＋／−」で地点数を増減し、施設ごと・端末ごとに保存する機能を実装。最低1地点、かつ保存されている最大センサ番号を下限とする。
- センサごとのA/B方向説明をボタン下に表示する仕組みは実装済み。実際の向きが未確定のため、方向説明は全地点で未設定。
- 記録は端末内に保存し、CSVの方向はA/Bで出力する。方向説明や対応表の版番号は記録・CSVには含まない。
- CSVの時刻をミリ秒付きの `timestamp` 1列へ統一。旧記録も保存済みのミリ秒情報を使って出力する。

## 改善・準備の候補

| 項目 | 内容 | 状況 |
| --- | --- | --- |
| 各地点の方向を確定 | 施設・センサ番号ごとに、Aの出発地点と到着地点、逆向きのBを決める。「右／左」ではなく展示室名などを使い、タップする通過位置も決める。現地で往復して確認後、共通設定へ反映する。 | 現地情報待ち |
| 方向対応表の保存・出力 | 施設・センサ番号・A/Bの意味・通過位置・版番号を対応表としてCSVと一緒に保管しやすくする。自動出力や記録時点の版番号保持は未実装。形式は別途検討する。 | 候補 |
| センサ数の増減画面 | 記録設定の「＋／−」で末尾の地点を増減する。記録済み番号より少なくできない安全策を入れ、施設別に端末内へ保存する。他端末には自動共有しない。 | 実装済み |
| 方向説明の編集・設定配布 | 方向説明も画面で管理できるようにする。全員の番号・方向の定義を揃えるため、共通設定の配布方法とセットで検討する。 | 候補 |
| 時刻の1列化と説明 | CSVをミリ秒付きtimestamp 1列へ統一し、CSV出力画面に形式と精度の注意を追加する。 | 実装済み |

## 現状の運用メモ

### timestamp_msとは

1970年1月1日00:00:00 UTCから経過したミリ秒数。1秒は1,000ミリ秒。
アプリではボタンのクリック処理の冒頭で時刻を取得する。CSVは `timestamp` 1列に統一し、互換性のため端末内部では次の2種類を保持する。

- `timestamp`：人が読める、タイムゾーン付きのミリ秒単位の時刻。例：`2026-09-11T12:43:27.123+09:00`。
- `timestamp_ms`：同じ瞬間を表す数値。上記の例なら `1789098207123`。CSVには出力しない。

2つのtimestamp_msの差を1,000で割ると、秒単位の時間差になる。時刻は端末の時計に依存し、自動的に他端末と同期する機能はない。ミリ秒単位で保存しても、実際の通過時刻をミリ秒精度で測定できるという意味ではない。

### センサ地点が増えたとき

記録設定で対象施設を選び、「センサ地点数」の「＋」を押す。
例えば国立科学博物館を20地点から25地点に増やす場合は5回押すと、センサ21〜25のA/Bボタンと履歴編集の選択肢が増える。25は固定の上限ではない。地点数の変更だけなら再公開は不要。

1. 既存の番号は変えず、末尾に新しい番号を追加する。新しい番号と実際の設置場所の対応を記録する。
2. 他端末には自動共有されないため、各端末で同じ地点数に設定する。
3. 方向説明も追加する場合は、HTMLの同じ施設の `directionLabels` にA/Bの説明を追加し、`sw.js` の版番号を上げて動作確認後に公開する。
4. 方向説明を更新した場合は、実験前に全端末でオンライン更新し、アプリの画面をすべて閉じて開き直す。番号と方向説明を確認する。

「−」で減らせるのは、その施設の保存済み記録の最大番号まで（最低1地点）。地点数の変更で過去の記録は削除しない。過去データの解釈が変わるため、既存番号の振り直しや別地点への使い回しは避ける。記録済み地点を画面から外す機能や、地点の廃止・設置場所の変更への対応は未実装で、履歴との対応を保つ方針を別途決める。
